# Contributing to FunPay Helper

Спасибо за интерес к проекту FunPay Helper! Мы рады любому вкладу в развитие расширения.

## 📋 Оглавление

- [Как начать](#как-начать)
- [Процесс разработки](#процесс-разработки)
- [Стиль кода](#стиль-кода)
- [Тестирование](#тестирование)
- [Отправка изменений](#отправка-изменений)
- [Сообщение о багах](#сообщение-о-багах)
- [Предложение функций](#предложение-функций)

## 🚀 Как начать

1. **Форкните репозиторий**
   ```bash
   git clone https://github.com/your-username/funpay-helper.git
   cd funpay-helper
   ```

2. **Создайте ветку для разработки**
   ```bash
   git checkout -b feature/my-new-feature
   ```

3. **Загрузите расширение в браузер**
   - Chrome: `chrome://extensions/` → "Режим разработчика" → "Загрузить распакованное"
   - Firefox: `about:debugging` → "Загрузить временное дополнение"

## 🔧 Процесс разработки

### Структура проекта

```
funpay-helper/
├── manifest.json              # Chrome Manifest V3
├── manifest_firefox.json      # Firefox Manifest V2
├── background/                # Фоновые скрипты
├── content/                   # Content scripts
├── popup/                     # Всплывающее окно
├── options/                   # Страница настроек
├── utils/                     # Вспомогательные утилиты
├── styles/                    # CSS стили
└── icons/                     # Иконки расширения
```

### Основные компоненты

- **background/service-worker.js**: Фоновые задачи, уведомления, проверка цен
- **content/inject.js**: Основная логика интеграции с FunPay
- **utils/storage.js**: Работа с chrome.storage и IndexedDB
- **utils/api.js**: API запросы к FunPay и бэкенду
- **utils/parser.js**: Парсинг данных со страниц FunPay

### Добавление новой функции

1. **Планирование**
   - Создайте issue с описанием функции
   - Обсудите реализацию с мейнтейнерами

2. **Реализация**
   - Следуйте существующим паттернам кода
   - Добавьте соответствующие настройки в options
   - Обновите README и FEATURES.md

3. **Тестирование**
   - Проверьте работу в Chrome и Firefox
   - Убедитесь, что не сломана существующая функциональность
   - Проверьте производительность

## 📝 Стиль кода

### JavaScript

```javascript
// Используйте async/await вместо .then()
async function fetchData() {
  try {
    const response = await fetch(url);
    return await response.json();
  } catch (error) {
    console.error('Error:', error);
  }
}

// Деструктуризация объектов
const { darkTheme, showQuickStats } = await chrome.storage.sync.get(['darkTheme', 'showQuickStats']);

// Стрелочные функции для коротких callback'ов
array.map(item => item.id);

// Именование: camelCase для переменных и функций
const userName = 'John';
function getUserData() { }

// Константы: UPPER_CASE
const MAX_TRACKERS = 3;
```

### CSS

```css
/* BEM нотация для классов */
.fph-stats-bar { }
.fph-stats-bar__item { }
.fph-stats-bar__item--active { }

/* Префикс fph- для всех классов расширения */
.fph-custom-element { }

/* Используйте CSS переменные */
:root {
  --fph-primary-color: #667eea;
  --fph-secondary-color: #764ba2;
}
```

### HTML

```html
<!-- Семантическая разметка -->
<section class="fph-section">
  <h2>Заголовок</h2>
  <article class="fph-article">
    <!-- контент -->
  </article>
</section>

<!-- Доступность -->
<button aria-label="Закрыть" title="Закрыть окно">×</button>
```

## 🧪 Тестирование

### Ручное тестирование

Перед отправкой PR проверьте:

- [ ] Работа в Chrome (последняя версия)
- [ ] Работа в Firefox (последняя версия)
- [ ] Все основные функции работают
- [ ] Нет ошибок в консоли браузера
- [ ] Производительность не ухудшилась
- [ ] Тёмная тема работает корректно
- [ ] Расширение не конфликтует с FunPay

### Тестовые сценарии

1. **Price Tracker**
   - Добавление отслеживания
   - Получение уведомлений
   - Просмотр графиков

2. **Seller Score**
   - Отображение рейтинга
   - Корректность расчёта
   - Цветовые индикаторы

3. **Auto-Responder**
   - Создание шаблонов
   - Вставка в чат
   - Горячие клавиши

## 📤 Отправка изменений

### Подготовка к PR

1. **Обновите свою ветку**
   ```bash
   git fetch origin
   git rebase origin/main
   ```

2. **Проверьте изменения**
   ```bash
   git status
   git diff
   ```

3. **Зафиксируйте изменения**
   ```bash
   git add .
   git commit -m "feat: добавить новую функцию X"
   ```

### Формат сообщений коммитов

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```
feat: добавить экспорт в PDF
fix: исправить ошибку расчёта рейтинга
docs: обновить README
style: форматирование кода
refactor: рефакторинг парсера
test: добавить тесты для API
chore: обновить зависимости
```

### Создание Pull Request

1. Отправьте ветку на GitHub
   ```bash
   git push origin feature/my-new-feature
   ```

2. Создайте PR через веб-интерфейс GitHub

3. **Заполните шаблон PR:**
   - Описание изменений
   - Связанные issues
   - Скриншоты (если применимо)
   - Чеклист тестирования

4. Дождитесь ревью от мейнтейнеров

## 🐛 Сообщение о багах

### Шаблон issue для бага

```markdown
**Описание бага**
Краткое описание проблемы

**Шаги для воспроизведения**
1. Перейти на '...'
2. Нажать на '...'
3. Увидеть ошибку

**Ожидаемое поведение**
Что должно было произойти

**Скриншоты**
Если применимо

**Окружение**
- Браузер: Chrome 120
- ОС: Windows 11
- Версия расширения: 1.0.0

**Дополнительный контекст**
Любая дополнительная информация
```

### Перед созданием issue

- Проверьте, нет ли уже такого же issue
- Убедитесь, что используете последнюю версию
- Проверьте консоль браузера на ошибки

## 💡 Предложение функций

### Шаблон issue для новой функции

```markdown
**Описание функции**
Чёткое описание предлагаемой функции

**Проблема, которую решает**
Какую задачу пользователя решает эта функция

**Предлагаемое решение**
Как вы видите реализацию

**Альтернативы**
Альтернативные варианты решения

**Дополнительный контекст**
Скриншоты, примеры из других расширений
```

## 🎨 Дизайн

При добавлении UI элементов:

- Следуйте существующему дизайну
- Используйте цветовую схему расширения
- Обеспечьте responsive дизайн
- Тестируйте в светлой и тёмной теме
- Учитывайте accessibility (a11y)

### Цветовая палитра

```css
/* Основные цвета */
--primary: #667eea;
--secondary: #764ba2;

/* Статусы */
--success: #4caf50;
--warning: #ff9800;
--error: #f44336;

/* Нейтральные */
--background: #ffffff;
--text: #333333;
--border: #e0e0e0;
```

## 📞 Связь

- **GitHub Issues**: Для багов и предложений
- **GitHub Discussions**: Для обсуждений
- **Email**: dev@funpayhelper.com
- **Telegram**: @funpayhelper_dev

## ⚖️ Лицензия

Отправляя PR, вы соглашаетесь с тем, что ваш код будет распространяться под лицензией MIT.

## 🙏 Спасибо

Спасибо всем контрибьюторам, которые помогают делать FunPay Helper лучше!

---

**Вопросы?** Не стесняйтесь создавать issue или писать в Discussions!
