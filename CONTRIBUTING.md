# Руководство по участию в проекте

Спасибо за интерес к проекту Funpay Competitor Analyzer! Мы рады любому вкладу в развитие расширения.

## Как внести вклад

### Сообщить об ошибке

Если вы обнаружили баг:

1. Проверьте, не создан ли уже issue на эту проблему
2. Создайте новый issue с подробным описанием:
   - Версия расширения
   - Браузер и его версия
   - Операционная система
   - Шаги для воспроизведения
   - Ожидаемое и фактическое поведение
   - Скриншоты (если применимо)
   - Логи из консоли браузера

### Предложить улучшение

Есть идея для новой функции?

1. Создайте issue с тегом "enhancement"
2. Опишите:
   - Что за функция
   - Зачем она нужна
   - Как должна работать
   - Примеры использования

### Внести код

1. **Fork репозитория**
   ```bash
   git clone https://github.com/your-username/funpay-analyzer.git
   cd funpay-analyzer
   ```

2. **Создайте ветку для изменений**
   ```bash
   git checkout -b feature/your-feature-name
   # или
   git checkout -b fix/bug-description
   ```

3. **Внесите изменения**
   - Следуйте стилю кода проекта
   - Добавьте комментарии где необходимо
   - Тестируйте изменения

4. **Commit изменений**
   ```bash
   git add .
   git commit -m "Добавлена функция X"
   ```

5. **Push в ваш fork**
   ```bash
   git push origin feature/your-feature-name
   ```

6. **Создайте Pull Request**
   - Опишите что изменено и зачем
   - Ссылка на связанный issue (если есть)
   - Скриншоты (если UI изменения)

## Стиль кода

### JavaScript

```javascript
// Используйте camelCase для переменных и функций
const myVariable = 'value';
function myFunction() { }

// Используйте const/let вместо var
const immutableValue = 10;
let mutableValue = 20;

// Пробелы вокруг операторов
const sum = a + b;

// Точка с запятой в конце выражений
const result = calculate();

// Понятные имена переменных
const competitorPrice = 100; // хорошо
const cp = 100; // плохо

// Комментарии только где необходимо
// Рассчитываем оптимальную цену с учетом 4 факторов
const optimalPrice = calculatePrice(factors);
```

### HTML

```html
<!-- Используйте семантические теги -->
<header>
  <h1>Заголовок</h1>
</header>

<main>
  <section>
    <article>Контент</article>
  </section>
</main>

<!-- Атрибуты в кавычках -->
<div class="container" id="main-content">

<!-- Правильная вложенность и отступы -->
<div class="parent">
  <div class="child">
    <span>Text</span>
  </div>
</div>
```

### CSS

```css
/* Используйте классы, избегайте ID для стилей */
.button { }

/* Группируйте связанные свойства */
.element {
  /* Позиционирование */
  position: relative;
  top: 0;
  
  /* Размеры */
  width: 100%;
  height: 50px;
  
  /* Отображение */
  display: flex;
  flex-direction: column;
  
  /* Оформление */
  background: #fff;
  border: 1px solid #000;
  border-radius: 8px;
}

/* Используйте CSS-переменные для повторяющихся значений */
:root {
  --primary-color: #667eea;
  --secondary-color: #764ba2;
}
```

## Структура проекта

```
funpay-analyzer/
├── manifest.json              # Конфигурация расширения
├── background/               
│   └── service-worker.js     # Фоновая логика
├── content/                  
│   ├── content-script.js     # Инъекция на Funpay
│   └── content-styles.css    # Стили для инъекции
├── popup/                    
│   ├── popup.html            # UI popup
│   ├── popup.css             # Стили popup
│   └── popup.js              # Логика popup
├── options/                  
│   ├── options.html          # UI настроек
│   ├── options.css           # Стили настроек
│   └── options.js            # Логика настроек
└── assets/                   
    └── icons/                # Иконки расширения
```

## Тестирование

Перед созданием PR:

1. **Загрузите расширение в браузер**
   - Проверьте в Chrome/Edge
   - Проверьте в Firefox (если изменения влияют на совместимость)

2. **Тестируйте функциональность**
   - Авторизация работает
   - Анализ конкурентов работает
   - UI отображается корректно
   - Настройки сохраняются

3. **Проверьте консоль**
   - Нет ошибок в консоли браузера
   - Нет предупреждений (warnings)

4. **Тестируйте на разных страницах**
   - Страницы товаров
   - Категории
   - Личный кабинет

## Области для вклада

### Приоритетные задачи

- [ ] Улучшение алгоритма рекомендаций цен
- [ ] Графики и визуализация данных
- [ ] История изменения цен
- [ ] Backend API для расширенной аналитики
- [ ] Экспорт данных (CSV, JSON)
- [ ] Автоматическое обновление цен

### Улучшения UI/UX

- [ ] Темная тема
- [ ] Дополнительные цветовые схемы
- [ ] Анимации и переходы
- [ ] Мобильная оптимизация
- [ ] Кастомизация положения элементов

### Документация

- [ ] Видео-туториалы
- [ ] FAQ
- [ ] Перевод на другие языки
- [ ] API документация

### Тестирование

- [ ] Unit тесты
- [ ] Integration тесты
- [ ] E2E тесты
- [ ] Тестирование производительности

## Правила коммуникации

1. **Будьте вежливы** - уважайте других участников
2. **Будьте конструктивны** - критикуйте код, не людей
3. **Будьте терпеливы** - reviewers работают в свободное время
4. **Будьте открыты** - готовы к обсуждению и изменениям

## Процесс review

1. Maintainer проверит ваш PR
2. Могут попросить внести изменения
3. После одобрения PR будет смержен
4. Ваш вклад будет упомянут в CHANGELOG

## Лицензия

Внося вклад, вы соглашаетесь, что ваш код будет под MIT License.

## Вопросы?

Если у вас есть вопросы:
- Создайте issue с тегом "question"
- Напишите в discussions (если включено)

---

**Спасибо за вклад в Funpay Competitor Analyzer!** 🎉
