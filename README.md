# Gang Helper для Arizona RP

Скрипт-хелпер для банд на Arizona RP сервере с GUI меню и автоматическими отыгровками.

## Возможности

### Основной функционал
- 🎮 GUI меню на базе mimgui с удобным интерфейсом
- 🎭 Автоматические отыгровки для всех банд-команд
- ⚙️ Гибкая настройка отыгровок для каждой команды
- 💾 Сохранение конфигурации в JSON
- 📝 Логирование всех действий

### Поддерживаемые команды

| Команда | Описание |
|---------|----------|
| `/capture` | Начать войну за территорию |
| `/captstats` | Статистика войн |
| `/putmoney` | Положить деньги на счёт притона |
| `/getmoney` | Снять деньги со счёта |
| `/setdbuy` | Установить цену за "укроп" в притоне |
| `/setdsell` | Установить цену скупки в притоне |
| `/stuff` | Набить татуировку |
| `/unstuff` | Свести татуировку |
| `/dropmats` | Выгрузить материалы из фуры |
| `/sellgangzone` | Продать территорию другой банде |

## Установка

1. Скопируйте файл `gang_helper.lua` в папку `moonloader`
2. Скопируйте папку `config` в корневую директорию moonloader
3. Перезапустите игру или перезагрузите скрипты (Ctrl+R)

## Использование

### Открытие меню
Введите команду в чат:
```
/gang
```

### Вкладки меню

#### 1. Команды банды
- Быстрый доступ ко всем банд-командам через кнопки
- Поля ввода для команд с параметрами
- Автоматическое выполнение отыгровок при нажатии

#### 2. Отыгровки
- Настройка текстов отыгровок перед командой (до 5 отыгровок)
- Настройка текстов отыгровок после команды (до 5 отыгровок)
- Сохранение настроек в конфиг

#### 3. Настройки
- Включение/выключение хелпера
- Включение/выключение автоматических отыгровок
- Включение/выключение логирования
- Настройка задержки между отыгровками (500-5000 мс)
- Сброс настроек к значениям по умолчанию

## Конфигурация

Настройки хранятся в файле `config/gang_config.json`:

```json
{
  "enabled": true,
  "delay_between_rp": 1500,
  "auto_rp_enabled": true,
  "log_actions": true,
  "commands": {
    "capture": {
      "enabled": true,
      "rp_before": [...],
      "rp_after": [...]
    }
  }
}
```

### Параметры конфигурации

- `enabled` - включен ли хелпер
- `delay_between_rp` - задержка между отыгровками в миллисекундах
- `auto_rp_enabled` - автоматические отыгровки
- `log_actions` - логирование действий в консоль
- `commands` - настройки для каждой команды
  - `enabled` - включена ли команда
  - `rp_before` - массив отыгровок перед командой
  - `rp_after` - массив отыгровок после команды

## Работа скрипта

1. При вводе банд-команды скрипт перехватывает её
2. Выполняет отыгровки перед командой (если настроены)
3. Выполняет саму команду
4. Выполняет отыгровки после команды (если настроены)
5. Логирует все действия (если включено)

## Требования

- MoonLoader
- SAMP.lua
- mimgui
- encoding
- jsoncfg

## Лицензия

Создано для Arizona RP сообщества.

## Автор

cto.new - Gang Helper v1.0.0

## Поддержка

Если у вас возникли проблемы или вопросы:
- Проверьте, что все библиотеки установлены
- Убедитесь, что конфиг-файл не повреждён
- Проверьте логи в moonloader.log
